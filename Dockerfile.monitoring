FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml .
COPY src ./src

RUN pip install --no-cache-dir .

EXPOSE 8502

CMD ["streamlit", "run", "src/toxic_mlops/monitoring/app.py", "--server.address=0.0.0.0", "--server.port=8502"]